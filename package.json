{
  "name": "kirciuokle",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "prebuild": "node scripts/check_spellcheck_assets.mjs",
    "build": "vite build",
    "build:client": "vite build",
    "check": "tsc --build && vitest run --config vitest.config.ts",
    "deploy:dev": "npm run build && wrangler deploy --config wrangler.jsonc --env dev",
    "deploy:prod": "npm run build && wrangler deploy --config wrangler.jsonc",
    "deploy": "echo 'Use deploy:dev (dev, auto) or deploy:prod (prod, requires explicit owner approval). See AGENTS.md.' && exit 1",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.43.0",
    "@cloudflare/workers-types": "^4.20260702.1",
    "@types/node": "^26.1.0",
    "typescript": "^6.0.3",
    "vite": "^8.1.3",
    "vitest": "^4.1.9",
    "wrangler": "^4.107.0"
  },
  "dependencies": {
    "hunspell-asm": "4.0.2"
  }
}
